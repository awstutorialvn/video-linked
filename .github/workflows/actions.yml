name: GitHub Actions Linked Video
run-name: ${{ github.actor }} ${{ github.repository }} Actions 🚀
        
on: [push]
jobs:
  Lint:
    name: Run Lint Code
    runs-on: ubuntu-latest
    steps:
      - name: Env Setup
        uses: awstutorialvn/video-linked/.github/workflows/build-env.yml

      - name: Lint Check
        run: yarn run lint

  Test:
    runs-on: ubuntu-latest
    steps:
      - name: Env Setup
        uses: awstutorialvn/video-linked/.github/workflows/build-env.yml

      - name: Lint Check
        run: yarn run test

  Build:
    runs-on: ubuntu-latest
    steps:
      - name: Env Setup
        uses: awstutorialvn/video-linked/.github/workflows/build-env.yml

      - name: CDK Build
        run: yarn run cdk synth